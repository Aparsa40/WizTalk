import React,{useState} from 'react'; import {AppState,Character} from '../types'; import {CharacterManager} from './CharacterManager'; import {X} from 'lucide-react';
interface SettingsProps{appState:AppState;characters:Character[];onCharactersChange:(characters:Character[])=>void;onUpdateState:(updates:Partial<AppState>)=>void;onClose:()=>void}
export function Settings({appState,characters,onCharactersChange,onUpdateState,onClose}:SettingsProps){const[voiceEnabled,setVoiceEnabled]=useState(appState.voiceEnabled);const[showManager,setShowManager]=useState(false);const save=()=>{onUpdateState({voiceEnabled});onClose()};return <><div className="fixed inset-0 z-40 flex items-center justify-center bg-black/70 p-4"><div className="relative w-full max-w-lg rounded-3xl border border-white/10 bg-[#2a1740] p-6 text-amber-50"><button onClick={onClose} className="absolute left-5 top-5"><X/></button><p className="text-xs uppercase tracking-[0.2em] text-amber-300/60">WizTalk Settings</p><h2 className="mt-1 text-2xl font-bold text-amber-200">تنظیمات گفت‌وگو</h2><div className="mt-7 space-y-5"><div className="rounded-2xl border border-white/10 bg-black/10 p-4"><p className="text-sm text-amber-100">مسیر پاسخ‌گویی</p><p className="mt-1 text-xs leading-5 text-amber-50/50">انتخاب Character تعیین می‌کند از چه تنظیمات داخلی و مسیر مدل استفاده شود. Provider و Model از رابط کاربری قابل تغییر نیستند.</p></div><label className="flex items-center justify-between rounded-2xl border border-white/10 bg-black/10 p-4"><span><span className="block text-sm">خواندن پاسخ با صدا</span><span className="mt-1 block text-xs text-amber-50/45">از تنظیمات Voice خود Character استفاده می‌شود.</span></span><input type="checkbox" checked={voiceEnabled} onChange={e=>setVoiceEnabled(e.target.checked)} className="h-5 w-5"/></label><button onClick={()=>setShowManager(true)} className="w-full rounded-xl border border-amber-300/30 px-4 py-3 text-sm text-amber-200">مدیریت شخصیت‌ها ({characters.length})</button><button onClick={save} className="w-full rounded-xl bg-amber-500 py-3 font-bold text-[#21102e]">ذخیره تنظیمات</button></div></div></div>{showManager&&<CharacterManager characters={characters} onChange={onCharactersChange} onClose={()=>setShowManager(false)}/>}</>;}